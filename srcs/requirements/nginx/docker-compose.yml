version: "3.6"
services:
 nginx:
  build: .
  container_name: nginx
  networks: 
    - network
  volumes:
    - /home/oagrram/data/wordpress:/var/www/wordpress
  ports:
   - "443:443"
networks:
  network:
    name: wp-network
    driver: bridge

